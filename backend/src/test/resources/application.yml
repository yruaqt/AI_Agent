spring:
  datasource:
    url: jdbc:h2:mem:starter_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  h2:
    console:
      enabled: false
app:
  version: test
  cors-origins: http://localhost:5173
  jwt-secret: test-secret-key-for-unit-testing-only-must-be-long-enough-256-bits-minimum
  jwt-expiration-seconds: 3600
